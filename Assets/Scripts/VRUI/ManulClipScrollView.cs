using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ManulClipScrollView : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
        //// Get the RectTransform of the item
        //RectTransform itemRect = GetComponent<RectTransform>();

        //// Get the RectTransform of the ScrollView's viewport
        //RectTransform viewportRect = transform.parent.parent.GetComponent<RectTransform>();
        //if(viewportRect.rect.Contains())
        //// Check if the item's bounds are contained within the viewport's bounds
        //bool isMasked = !RectTransformUtility.RectangleIntersectsRect(viewportRect, itemRect);


    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
